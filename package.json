{
  "name": "donghaeng-lotto",
  "packageManager": "yarn@4.6.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace client start\" \"yarn workspace server dev\"",
    "build": "yarn workspace client build && yarn workspace server build"
  }
}
